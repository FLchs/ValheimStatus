name: Auto Release

on:
  push:
    branches: [master]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      skipped: ${{ steps.changelog.outputs.skipped }}
      tag: ${{ steps.changelog.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Conventional Changelog Action
        id: changelog
        uses: TriPSs/conventional-changelog-action@v5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          version-file: "package.json"
          output-file: "CHANGELOG.md"
          skip-tag: false
          preset: angular
          release-count: 0
          git-user-name: "Release bot"
          git-user-email: "bot@release.local"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: ${{ steps.changelog.outputs.skipped == 'false' }}
        with:
          tag_name: ${{ steps.changelog.outputs.tag }}
          name: ${{ steps.changelog.outputs.tag }}
          body: ${{ steps.changelog.outputs.clean_changelog }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build:
    needs: release
    if: ${{ needs.release.outputs.skipped == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - name: Create .env file
        run: |
          echo "VITE_API_URL=${{ secrets.VITE_API_URL }}" >> .env
          echo "VITE_SERVER_ADDRESS=${{ secrets.VITE_SERVER_ADDRESS }}" >> .env
          echo "VITE_APP_VERSION=${{ needs.release.outputs.tag }}" >> .env
      - run: bun install
      - run: bun run build
      - name: Create dist archive
        run: zip -r dist.zip dist/
      - name: Upload to release
        uses: softprops/action-gh-release@v1
        with:
          files: dist.zip
          tag_name: ${{ needs.release.outputs.tag }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 90

  screenshot:
    needs: [build, release]
    if: ${{ needs.release.outputs.skipped == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - run: bun install
      - name: Install Playwright
        run: bunx playwright install chromium
      - name: Generate Screenshot
        run: |
          bunx run preview &
          sleep 30
          bunx playwright screenshot --viewport-size=800,1400 --wait-for-timeout=3000 "http://localhost:4173/#/demo" screenshot.png
          pkill -f "serve" || true
      - name: Update README Screenshot
        run: |
          git config user.name "Release bot"
          git config user.email "bot@release.local"
          mv screenshot.png docs/screenshot.png
          git add docs/screenshot.png
          git commit -m "docs: update screenshot for release ${{ needs.release.outputs.tag }}"
          git push

  deploy:
    needs: [build, screenshot]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
